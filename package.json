{
  "name": "root",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^6.2.0",
    "prettier": "^2.3.2"
  },
  "scripts": {
    "build": "yarn workspace @kickstartds/storybook-addon-component-tokens build",
    "dev": "concurrently \"yarn workspace @kickstartds/storybook-addon-component-tokens dev\" \"yarn workspace examples storybook\"",
    "format": "prettier -w ."
  }
}
