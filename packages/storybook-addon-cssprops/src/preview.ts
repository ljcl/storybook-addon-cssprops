import { ProjectAnnotations, Renderer } from "storybook/internal/types";

/** Here we could include things like decorators or initialGlobals */
const preview: ProjectAnnotations<Renderer> = {};

export default preview;
